# Generated capabilities

Live discovery saves verified artifacts here by default, for example `read-savings.json`.
`cua export-example` can also write a development copy here.

The bundled hand-authored fixture lives in
[`test_environment/fixtures/capabilities/`](../test_environment/fixtures/capabilities/).
Sample inputs and the MockBank policy are grouped in `test_environment/` as well.
